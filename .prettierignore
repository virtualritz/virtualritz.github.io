# Vendored third-party bundle (justif) carrying hand-applied SITE PATCH
# hunks (commit 32f5014) fixing a real upstream bug. The project's global
# Prettier PostToolUse hook reformats any file it touches; reformatting
# this bundle would produce an enormous spurious diff and make the
# patches impossible to re-apply cleanly after a version bump.
static/js/lib/

# Generated by build/fonts.py from live font metrics; hand-formatting
# would just be undone by the next font rebuild.
static/fonts/manifest.json

# Zola build output — generated, and already gitignored.
public/
