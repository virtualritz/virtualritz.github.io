# Zola build output
/public/

# Stale artifact from an earlier Hugo attempt
.hugo_build.lock

# Local Claude Code state
/.claude/settings.local.json
/.claude/audit/
# ...and anywhere else: the audit hook writes relative to the shell's cwd,
# so a command run from a subdirectory drops one there instead.
.claude/audit/
# Claude Code worktrees and local state
/.claude/worktrees/

# SDD scratch workspace
/.superpowers/

# Build artifacts
/build/.fontcache/

# node
/node_modules/
/tests/.out/
